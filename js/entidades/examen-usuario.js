function examen_usuario(id, examen, usuario, fecha, calificacion, ejecucion){
    this.id = id;
    this.examen = examen;
    this.usuario = usuario;
    this.fecha = fecha;
    this.calificacion = calificacion;
    this.ejecucion = ejecucion;
}