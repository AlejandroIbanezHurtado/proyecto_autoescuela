function examen(id, descripcion, duracion, num_preguntas, activo){
    this.id = id;
    this.descripcion = descripcion;
    this.duracion = duracion;
    this.num_preguntas = num_preguntas;
    this.activo = activo;
}